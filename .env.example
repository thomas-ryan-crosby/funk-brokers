# Copy to .env and fill in. Used for: local dev (npm run dev / vercel dev), migration script.
# Vercel uses its own Environment Variables in the dashboard — you don't need .env in production.

# --- Neon (Wave 4 social) ---
DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require

# --- API routes (server-side when running vercel dev) ---
ATTOM_API_KEY=
GOOGLE_MAPS_API_KEY=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# --- Client (Vite; only VITE_* are exposed to the browser) ---
VITE_USE_SOCIAL_READS=true
VITE_USE_SERVER_DATA_LAYER=true
VITE_USE_ATTOM_CACHE=true
# VITE_API_BASE=http://localhost:5173
# VITE_GOOGLE_MAPS_API_KEY=
# VITE_PERSONA_ENV_ID=
# VITE_PERSONA_TEMPLATE_ID=
# VITE_PERSONA_FORCE_HOSTED=false
